package com.example.shoppinglist2app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Sky Blue Light Theme ────────────────────────────────────────────────────
val SkyBluePrimary   = Color(0xFF0284C7)   // sky-600
val SkyBlueDark      = Color(0xFF0C4A6E)   // sky-950 (text)
val SkyBlueMedium    = Color(0xFF0369A1)   // sky-700 (secondary text)
val SkyBlueLight     = Color(0xFFE0F2FE)   // sky-100 (background)
val SkyBlueSurface   = Color(0xFFF0F9FF)   // sky-50  (card)
val SkyBlueAccent    = Color(0xFF7DD3FC)   // sky-300
val SkyBlueCard      = Color(0xFFFFFFFF)   // white card bg
val SkyBlueBorder    = Color(0xFFBAE6FD)   // sky-200
val SkyBlueNavBg     = Color(0xFF93C5FD)   // blue-300 — nav bar (diff shade)
val SkyBlueNavDark   = Color(0xFF1E40AF)   // blue-800 — nav selected
val SkyBlueTeal      = Color(0xFF0891B2)   // cyan-600 accent

// ── Category Colors ─────────────────────────────────────────────────────────
val CategoryVegetables = Color(0xFF4CAF50)  // 🟢 Green
val CategoryDairy      = Color(0xFF2196F3)  // 🔵 Blue
val CategoryMeat       = Color(0xFFF44336)  // 🔴 Red
val CategorySnacks     = Color(0xFFFFC107)  // 🟡 Yellow
val CategoryBeverages  = Color(0xFF9C27B0)  // 🟣 Purple
val CategoryHousehold  = Color(0xFFFF9800)  // 🟠 Orange
val CategoryBakery     = Color(0xFF795548)  // Brown
val CategoryFrozen     = Color(0xFF00BCD4)  // Cyan
val CategoryPersonal   = Color(0xFFE91E63)  // Pink
val CategoryGeneral    = Color(0xFF607D8B)  // Blue-grey

// ── Priority Colors ──────────────────────────────────────────────────────────
val PriorityHigh   = Color(0xFFEF4444)
val PriorityMedium = Color(0xFFFBBF24)
val PriorityLow    = Color(0xFF22C55E)
